export default function ProductView(currentProduct)
{
    console.log(currentProduct)


    return (
        
        <div className="product-view">
          {this.currentProduct!=null && <div>Hi</div>}
            
        </div>
    )
}